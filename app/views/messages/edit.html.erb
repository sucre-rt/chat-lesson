<%= form_with model: @message, url: message_path, method: :patch, local: true, class: "edit-form" do |f| %>
	<%= f.text_area :content, value: @message.content, class: "edit-content" %>
	<%= f.submit "変更" %>
<% end %>